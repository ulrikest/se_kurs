[project]
name = "uli_vicsek"
version = "1.0.0"
requires-python = ">=3.8"
description = "My first Python package"
authors = [
  {name = "Ulrike Stöffelmair", email = "ulrike.stoeffelmair@dlr.de"}]
dependencies = ["numpy", "matplotlib", "click", "logging", "pytest", "pathlib"]
